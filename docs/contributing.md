# Contributing

## Development Setup
```bash
pip install -r requirements-dev.txt
```

## Quality Checks
See `AGENTS.md` for the full quality checklist and process requirements.

## Process
- Important changes require a plan and design doc (see `AGENTS.md`).
- Keep documentation in `docs/` up to date.
